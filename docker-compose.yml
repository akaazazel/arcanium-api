volumes:
  postgres-data:

services:
  fastapi:
    build: .
    ports:
      - 8000:8000

  postgres:
    image: postgres:14.23-alpine3.23
    env_file:
      - .env
    ports:
      - 5431:5432
    volumes:
      - postgres-data:/var/lib/postgresql/data
